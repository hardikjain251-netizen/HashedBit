# React Calculator

A simple calculator built with React (Vite), taking two numbers and performing
addition, subtraction, multiplication, and division.

## How to run locally

```bash
npm install
npm run dev
```

Then open the URL shown in the terminal (usually http://localhost:5173) in your browser.

## Build for production

```bash
npm run build
```